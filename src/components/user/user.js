import React from 'react';

const UserButtons = () => {
    return (
        <div>
            <button>zaloguj</button>
            <button>załóż konto</button>

        </div>
    );
};

export default UserButtons;
